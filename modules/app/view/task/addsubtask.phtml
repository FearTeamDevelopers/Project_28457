<div class="box-content">
    <form class="form" action="/task/addsubtask/{echo $taskid}" method="post">
        <input type="hidden" name="tk" value="{echo $token}" />
        {if !empty($tasks)}
        <div class="form-row">
            <p class="form-label">Subtask</p>
            <div class="form-item">
                <select name="subtask">
                    {foreach $task in $tasks}
                    <option value="{echo $task->getId()}">{echo $task->getTitle()}
                    {/foreach}
                </select>
            </div>
        </div>
        <div class="form-row">
            <div class="form-item">
                <input type="submit" class="button blue" name="submitAddSubtask" value="Add subtask"/>
            </div>
        </div>
        {/if}{else}
        <div class="form-row">
            <p class="form-label">No task found</p>
            <div class="form-item">
            </div>
        </div>
        {/else}
    </form>
</div>